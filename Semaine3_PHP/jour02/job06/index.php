<?php
                    // Exercices sur les conditions pour afficher un vulgaire rectangle

// Faire un algorithme qui affiche un rectangle de 20 de largeur par 10 de hauteur. 
// Ces dimensions devront être stockées dans deux variables $largeur et $hauteur, modifiables facilement.

$largeur = 20;
$hauteur = 10;

for ( $i = 0; $i < $hauteur; $i++ ) {

    for ( $n = 0; $n < $largeur; $n++ ) {
        echo "*" . "" . "*";
    }
    
    echo "<br>";
}

// Je n'y arrive pas comme il l'entend mais il est bien quand meme ce rectangle je trouve..

?>